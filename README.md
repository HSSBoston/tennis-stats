## Event-Driven Gain in Expectancy (EDGE): A Probablistic Metric for Comprehensive Player Performance in Tennis

Traditional tennis statistics, such as aces, double faults, winners, and unforced errors, provide intuitive box-score summaries of player performance. However, these primitive metrics evaluate isolated aspects of performance rather than summarizing a player's overall performance in a single measure. As a result, comparing individual players often requires tables or other visualizations that combine multiple separate metrics. This makes interpretation less direct.

Composite metrics address this limitation by combining multiple aspects of play into a single numerical measure. For example, metrics such as Dominance Ratio (DR) and hold-plus-break percentage summarize performance more directly by combining serve and return outcomes. However, since these metrics deterministically aggregate observed outcomes without assigning score-dependent value to each point or event, they cannot distinguish between events that have different impacts on the probability of winning the current game. For example, a winner at 40–0 and a winner on break point often have different effects on the game, even though both are recorded as one winner.

 they cannot 

these metrics are deterministic: they are calculated from observed outcomes and do not assign score-dependent value to each point or event.



 according to its impact on a future outcome, such as winning the game or match. For example, a winner at 40–0 and a winner on break point often have different effects on the game, even though both are recorded as one winner.

Unlike these deterministic metrics, this project studies a probabilistic metric, called Event-Driven Gain in Expectancy (EDGE), which quantifies the score-dependent value of point-level events (e.g., aces, winners, and unforced errors) based on their probabilistic impacts on game-win expectancy. 


its expected impact on a future outcome, such as winning the current game.

fully account for the score context in which each point occurs. 

Unlike 


match-level outcomes or point-level events (e.g., observed serve/return/point outcomes) 
